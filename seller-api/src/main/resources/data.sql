CREATE TABLE tb_seller
(
    id     BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name   VARCHAR(255)                            NOT NULL,
    salary DOUBLE PRECISION                        NOT NULL,
    bonus  DOUBLE PRECISION,
    gender INT                                     NOT NULL,
    CONSTRAINT pk_tb_seller PRIMARY KEY (id)
);


INSERT
INTO TB_SELLER(NAME,SALARY,BONUS,GENDER)
VALUES('João', 5999.99,13,0);

INSERT
INTO TB_SELLER(NAME,SALARY,BONUS,GENDER)
VALUES('Bruna', 4999.99,20,1);

INSERT
INTO TB_SELLER(NAME,SALARY,BONUS,GENDER)
VALUES('Romário',2000.00,50,0);
